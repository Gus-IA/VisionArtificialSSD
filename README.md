# SSD Object Detection with PyTorch

Este proyecto implementa un modelo SSD (Single Shot Multibox Detector) para la detección de objetos usando el conjunto de datos Pascal VOC. El modelo está basado en la arquitectura de redes neuronales convolucionales (CNN) y utiliza técnicas como el *anchor generation* y *bounding box regression*.

## Requisitos

Este proyecto utiliza las siguientes librerías de Python:

- `torch` y `torchvision` para las redes neuronales y la manipulación de imágenes.
- `albumentations` para la augmentación de datos.
- `matplotlib` para la visualización de imágenes y los *bounding boxes*.
- `numpy` para manipulaciones numéricas de arrays.

🧩 Requisitos

Antes de ejecutar el script, instala las dependencias:

pip install -r requirements.txt

🧑‍💻 Autor

Desarrollado por Gus como parte de su aprendizaje en Python e IA.
